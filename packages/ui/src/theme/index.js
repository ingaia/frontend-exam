import {
  useTheme as ut,
  withTheme as wt,
  Theme as T,
} from './theme';

export const useTheme = ut;
export const withTheme = wt;
export const Theme = T;

export default {
  useTheme,
  withTheme,
  Theme,
};
